{
  "name": "memory-match",
  "version": "1.0.0",
  "description": "A fun memory matching card game",
  "main": "script.js",
  "scripts": {
    "start": "http-server -p 3000",
    "generate-icons": "node generate_icon.js",
    "generate-sounds": "node generate_sounds.js",
    "prepare": "npm run generate-icons && npm run generate-sounds"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "canvas": "^2.11.2"
  }
}
