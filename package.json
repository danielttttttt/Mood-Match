{
  "name": "memory-match",
  "version": "1.0.0",
  "description": "A fun memory matching card game",
  "main": "script.js",
  "scripts": {
    "start": "http-server -p 3000",
    "lint": "eslint . --ext .js",
    "format": "prettier --write .",
    "generate-icons": "node generate_icon.js",
    "generate-sounds": "node generate_sounds.js",
    "prepare": "npm run lint"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "eslint": "^8.59.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "prettier": "^3.3.1"
  }
}
